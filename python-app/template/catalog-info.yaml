apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Python app that displays time named ${{values.app_name}}
    github.com/project-slug: DiogoMajela/${{values.app_name}}
spec:
  type: service
  owner: development
  lifecycle: experimental
